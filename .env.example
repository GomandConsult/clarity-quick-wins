# Resend (https://resend.com) — used to send the diagnostic report email.
# Requires the sending domain (notify.gomandconsult.com) to be verified in Resend.
RESEND_API_KEY=

# Optional: send a BCC copy of every report (e.g. for QA in preview deploys).
SEND_REPORT_BCC=
